import { MAX_CALCULATE_NUMBER, MAX_DIGIT_PLACE } from '.';

export const ALERT_MESSAGE = {
  MAX_DIGIT_COUNT: `${MAX_DIGIT_PLACE}자리의 숫자만 입력이 가능합니다`,
  NUMBER_FIRST: '숫자를 먼저 입력한 후 연산자를 입력해주세요',
  MAX_OPERATOR_COUNT: `${MAX_CALCULATE_NUMBER + 1}개의 숫자에 대한 연산만 가능합니다`,
};
